#!/bin/bash

UTILITYS="repo  repo_google  xxcleandavid  xxfree  xxlistfile  xxproduct  xxsyncbuild xxbaseversion find_ex grep_ex xxinfo"

for TOOL in ${UTILITYS}
do
	echo -e "/work/github/utility/${TOOL} -> ~/bin/${TOOL}"
	ln -sf /work/github/utility/${TOOL} ~/bin/${TOOL}
done
