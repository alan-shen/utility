#!/bin/bash

UTILITYS="repo  repo_google  xxcleandavid  xxfree  xxlistfile  xxproduct  xxsyncbuild"

for TOOL in ${UTILITYS}
do
	echo -e "${PWD}/${TOOL} -> ~/bin/${TOOL}"
	ln -sf ${PWD}/${TOOL} ~/bin/${TOOL}
done
